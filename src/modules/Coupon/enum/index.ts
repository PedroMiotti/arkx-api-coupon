export enum CouponResponse {
  ISSUED = 'ISSUED',
  REACHED_LIMIT = 'REACHED_LIMIT',
  INVALID = 'INVALID',
}

export enum CouponTier {
  THIRTY_PERCENT = 1,
  FIFTY_PERCENT = 2,
  HUNDRED_PERCENT = 3,
}
